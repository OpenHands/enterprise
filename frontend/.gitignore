# i18n translation files make by script using `make build`
public/locales/**/*
src/i18n/declaration.ts
.env
node_modules/
/test-results/
/playwright-report/
/blob-report/
/playwright/.cache/
.react-router/
ralph/

# Agent Canvas SPA staged by scripts/build-agent-canvas.sh (temporary: /canvas
# is served from the cloud image as a separate service). Generated, never committed.
/public/canvas/
/.cache/
