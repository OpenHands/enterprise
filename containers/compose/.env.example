# Copy to .env, supply unique secrets, and keep the file private (chmod 600).
# Do not print `docker compose config` with real secrets; use `config --quiet`.
ENTERPRISE_DB_PASSWORD=
AUTOMATION_DB_PASSWORD=
AUTOMATIONS_SERVICE_KEY=
AUTOMATION_WEBHOOK_SECRET=
AUTOMATION_KV_SECRET=
PUBLIC_HOST=localhost:8443
# Reserve the proxy IP within this subnet; change both if your network overlaps.
COMPOSE_SUBNET=172.30.44.0/24
PROXY_IPV4_ADDRESS=172.30.44.10
# --- Auto-TLS Caddy overlay (caddy-autotls.yaml) ---
# Local internal CA (default) -> offline, no DNS. Set to your public domain for
# automatic Let's Encrypt (needs public DNS + inbound 80/443). Keep PUBLIC_HOST
# in sync with SITE_ADDRESS for hosted mode.
SITE_ADDRESS=localhost
# Published ports for the auto-TLS overlay. Hosted/ACME requires 80 and 443;
# for local you may map high ports (e.g. HTTP_PORT=8080 HTTPS_PORT=8443).
HTTP_PORT=8080
HTTPS_PORT=8443
KEYCLOAK_SERVER_URL=
KEYCLOAK_SERVER_URL_EXT=
KEYCLOAK_REALM_NAME=
KEYCLOAK_CLIENT_ID=
KEYCLOAK_CLIENT_SECRET=
KEYCLOAK_ADMIN_PASSWORD=
