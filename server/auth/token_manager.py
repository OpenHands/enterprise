"""Compatibility imports; implementation lives inside the Keycloak adapter."""

from server.auth.keycloak.token_manager import (
    KeycloakUserInfo as KeycloakUserInfo,
)
from server.auth.keycloak.token_manager import (
    TokenManager as TokenManager,
)
