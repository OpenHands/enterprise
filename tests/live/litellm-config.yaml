model_list:
  - model_name: budget-test
    litellm_params:
      model: openai/gpt-4o-mini
      api_key: sk-local-test-not-a-provider-key
      mock_response: budget test response
    model_info:
      # Binary-exact prices prevent rounding from accidentally enforcing equality.
      input_cost_per_token: 0.125
      output_cost_per_token: 0.125
general_settings:
  master_key: os.environ/LITELLM_MASTER_KEY
  database_url: os.environ/DATABASE_URL
litellm_settings:
  telemetry: false
